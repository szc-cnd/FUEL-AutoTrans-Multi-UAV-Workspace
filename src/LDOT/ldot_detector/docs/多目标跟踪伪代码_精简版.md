# 多目标跟踪模块伪代码（精简版）

```
输入: P_t (点云), Tracks_{t-1} (历史轨迹), dt (时间步长)
输出: Tracks_t (更新后的轨迹集合)
```

```python
function MultiObjectTracking(P_t, Tracks_{t-1}, dt):
    # 步骤1: 自适应聚类 (公式1)
    kdtree = BuildKDTree(P_t)
    Clusters = AdaptiveDBSCAN(P_t, kdtree, ε_base, α, N_min)
    
    # 步骤2: 异常边界框过滤 (公式2)
    ValidDetections = FilterBySize(Clusters, w_max)
    
    # 步骤3: 鲁棒数据关联 (公式3)
    PredictedTracks = PredictAllTracks(Tracks_{t-1}, dt)
    CostMatrix = ComputeCostMatrix(ValidDetections, PredictedTracks)
    Matches = HungarianAlgorithm(CostMatrix)
    
    # 步骤4: 几何特征分类 (公式4)
    ClassifyAndSwitchModel(Matches, ValidDetections, Tracks_{t-1})
    
    # 步骤5: 自适应状态估计 (公式5-7)
    UpdatedTracks = []
    for (det_idx, track_idx) in Matches:
        x_updated, P_updated = KalmanUpdate(detection, x_pred, P_pred)
        R_hat, Q_hat = AdaptiveNoiseEstimation(track.innovation_history)
        track.R = (1-λ) × track.R + λ × R_hat
        track.Q = (1-λ) × track.Q + λ × Q_hat
        track.state = x_updated
        UpdatedTracks.append(track)
    
    # 步骤6: 轨迹管理
    Tracks_t = ManageTracks(UpdatedTracks, UnmatchedTracks, UnmatchedDetections)
    
    return Tracks_t
```
