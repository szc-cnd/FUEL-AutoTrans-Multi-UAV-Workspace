# 热成像到 D435 单应矩阵标定启动命令

标定时不要启动比赛七分屏和融合节点。

## 终端 1：启动 ROS

```bash
source /opt/ros/noetic/setup.bash
roscore
```

## 终端 2：启动 D435

```bash
cd ~/match_ws
source devel/setup.bash
roslaunch realsense2_camera rs_camera.launch \
  align_depth:=true \
  enable_color:=true \
  enable_depth:=true \
  color_width:=1280 \
  color_height:=720 \
  depth_width:=1280 \
  depth_height:=720 \
  enable_pointcloud:=false
```

## 终端 3：启动热成像

```bash
cd ~/match_ws
source devel/setup.bash
roslaunch uvc_ubuntu thermal_detector.launch
```

## 终端 4：启动标定窗口

```bash
cd ~/match_ws
source devel/setup.bash
rosrun uvc_ubuntu thermal_d435_calib_node.py \
  _config_path:="$(rospack find uvc_ubuntu)/config/thermal_d435_fusion.yaml"
```

窗口按键：

```text
r：清空采样点
e：查看误差
s：保存矩阵
q：退出
```

每个位置先点击左侧热成像中的固定特征，再点击右侧 D435 中的同一特征。保存位置：

```text
~/match_ws/src/uvc_ubuntu/config/thermal_d435_fusion.yaml
```

## 标定后测试

### 终端 4：启动融合

```bash
cd ~/match_ws
source devel/setup.bash
rosrun uvc_ubuntu thermal_d435_fusion_node.py \
  _config_path:="$(rospack find uvc_ubuntu)/config/thermal_d435_fusion.yaml" \
  _use_tf:=false
```

### 终端 5：查看 D435 映射画面

```bash
source ~/match_ws/devel/setup.bash
rqt_image_view /UAV0/thermal/d435_debug_image
```

### 查看融合状态

```bash
rostopic echo /UAV0/thermal/fusion_valid
```
