# 热成像到 D435 单应矩阵标定流程

## 适用配置

本流程适用于当前 UAV0 相机配置：

```text
热成像：384×288
D435 彩色：1280×720
D435 深度：1280×720
深度对齐到彩色图：开启
```

单应矩阵只适用于标定距离附近的同一平面。它用于把热成像像素映射到 D435 彩色像素，再查询对齐深度。若目标距离变化很大，需要后续进行热成像内参和双相机外参标定。

## 一、准备标定热源

推荐使用一个容易移动的小热源：

- 装有 50～70°C 热水的密封薄壁金属容器；
- 容器外壁贴一小块哑光黑胶带或清晰十字标记；
- 热成像和 D435 都以同一个固定物理特征中心为点击位置。

也可以使用只开启一根加热管的取暖器，但必须安全断电后移动、放稳后再通电，不要手持正在工作的取暖器。

标定时：

- 无人机、D435 和热成像相机必须固定；
- 两台相机之间不能发生相对移动；
- 选比赛常用检测距离作为标定距离，例如约 1 m；
- 热源只在该距离的同一平面内上下左右移动；
- 前后距离变化尽量控制在 2～3 cm 内；
- 每次点击前等待热源和画面稳定。

## 二、启动相机

标定时不要启动比赛七分屏，也不要启动融合节点，避免重复占用相机或混淆结果。

### 终端 1：ROS Master

```bash
source /opt/ros/noetic/setup.bash
roscore
```

如果已经有 `roscore`，不要重复启动。

### 终端 2：D435

```bash
cd ~/match_ws
source devel/setup.bash
roslaunch realsense2_camera rs_camera.launch \
  align_depth:=true \
  enable_color:=true \
  enable_depth:=true \
  color_width:=1280 \
  color_height:=720 \
  depth_width:=1280 \
  depth_height:=720 \
  enable_pointcloud:=false
```

### 终端 3：热成像检测

```bash
cd ~/match_ws
source devel/setup.bash
roslaunch uvc_ubuntu thermal_detector.launch
```

### 检查图像话题

```bash
rostopic hz /UAV0/thermal/debug_image
rostopic hz /camera/color/image_raw
rostopic hz /camera/aligned_depth_to_color/image_raw
```

三个话题都应有稳定帧率。

如果热成像提示 `hik_capture_open error`，先检查海康 USB 设备权限和设备是否被其他进程占用。

## 三、启动单应矩阵标定

```bash
cd ~/match_ws
source devel/setup.bash
rosrun uvc_ubuntu thermal_d435_calib_node.py \
  _config_path:="$(rospack find uvc_ubuntu)/config/thermal_d435_fusion.yaml"
```

窗口中：

```text
左侧：热成像调试图
右侧：D435 彩色图
```

按键：

```text
r：清空本次所有采样点
e：计算并显示重投影误差
s：保存单应矩阵
q 或 ESC：退出
```

启动后先按一次 `r`，确保没有本次误点。

## 四、选择采点范围

只需一个热源，把同一个固定物理特征依次移动到不同区域。需要覆盖的是“两台相机共同可见且比赛目标可能出现”的区域，不必强行覆盖 D435 或热成像的全部画面。

建议先采用 3×3 分布，共 9 对点：

```text
左上    上中    右上
左中    中心    右中
左下    下中    右下
```

如果公共视场允许，可在区域之间增加 3～7 对点，总计约 12～16 对。均匀覆盖比点数多更重要。

热成像检测有效 ROI 约为：

```text
x = 30～354
y = 20～268
```

若某个预定位置只有一台相机能看到，应向公共视场中心移动，直到左右画面都能清楚看到同一特征。

## 五、逐对点击

每个位置严格执行：

1. 将热源移动到新的区域，并保持标定距离基本不变；
2. 等待热源完全静止、热图稳定；
3. 点击左侧热成像中的固定物理特征中心；
4. 点击右侧 D435 中同一个物理特征中心；
5. 确认左右编号一致，再移动到下一个区域。

注意：

- 始终先点热成像，再点 D435；
- 每对必须对应同一个真实物理点；
- 不要一会儿点整框中心、一会儿点灯管顶部；
- 不要在同一局部区域连续采很多点；
- 不要混合明显不同的目标距离；
- 当前工具不能单独撤销最后一点，点错后按 `r` 清空重做。

## 六、检查误差

采集完成后按 `e`。终端会显示：

```text
Computed H with 内点数/总点数
mean error
max error
quality
```

建议满足：

```text
RANSAC 内点比例 ≥ 80%
平均重投影误差 ≤ 3 px
最大重投影误差 ≤ 8～10 px
```

即使误差很低，如果所有点集中在小区域，结果仍不可用。必须同时检查采样覆盖范围。

如果误差或内点比例不合格，不要保存，按 `r` 清空后重新采集。

## 七、保存结果

确认误差和覆盖范围合格后按 `s`。终端应出现：

```text
Saved homography to .../thermal_d435_fusion.yaml
```

保存文件为：

```text
/home/asus/match_ws/src/uvc_ubuntu/config/thermal_d435_fusion.yaml
```

保存字段为：

```yaml
thermal_to_d435_homography:
  - [...]
  - [...]
  - [...]
```

按 `q` 退出标定窗口。融合节点只在启动时读取矩阵，所以保存后必须重新启动融合节点。

## 八、单独验证融合结果

保持 D435 和热成像节点运行，启动融合节点，但关闭通道坐标 TF：

```bash
cd ~/match_ws
source devel/setup.bash
rosrun uvc_ubuntu thermal_d435_fusion_node.py \
  _config_path:="$(rospack find uvc_ubuntu)/config/thermal_d435_fusion.yaml" \
  _use_tf:=false
```

打开 D435 映射调试画面：

```bash
source ~/match_ws/devel/setup.bash
rqt_image_view /UAV0/thermal/d435_debug_image
```

画面含义：

- 多边形：热源检测框映射到 D435 后的区域；
- 十字：映射中心点；
- 圆圈：深度搜索范围；
- 绿色：映射位置在画面内且深度有效；
- 红色：映射越界或深度无效；
- 顶部第一行：热成像像素、D435 像素和深度；
- 顶部第二行：D435 光学坐标系下的 `XYZ`，单位 m。

同时检查：

```bash
rostopic echo /UAV0/thermal/fusion_valid
rostopic echo /UAV0/thermal/target_d435_pixel
rostopic echo /UAV0/thermal/target_camera_point
```

有效时应看到：

```text
fusion_valid: True
```

## 九、验证位置

将热源保持在标定距离，依次放到未参与标定的中心、四角和边缘位置，检查：

- 十字是否落在 D435 中真实热源的对应位置；
- 映射框是否包围目标；
- 框可以呈轻微梯形，但不能异常倾斜、拉长或放大；
- 深度是否稳定有效；
- `D435 XYZ` 是否连续、合理。

然后在比赛可能出现的近、中、远距离分别验证。单应矩阵在标定距离附近最准，距离变化导致的视差不属于重投影误差，必须通过实际测试评估。

## 十、常见问题

### 中心点正确但框严重变形

通常是标定点集中在局部，框四角落在采样范围外，单应矩阵外推失真。应重新标定并均匀覆盖公共视场。

### 框是不规则四边形

矩形经过单应变换后成为透视四边形是正常现象。轻微梯形可以接受；明显斜拉、翻转或异常放大不正常。

### 显示红色或 `fusion_valid=False`

检查：

```bash
rostopic hz /camera/aligned_depth_to_color/image_raw
rostopic echo /UAV0/thermal/target_d435_pixel
```

常见原因是映射点越界、映射位置没有有效深度，或 D435 内参与深度图尚未收到。

### 改变相机分辨率后还能使用吗

不能保证。当前单应矩阵绑定以下分辨率：

```text
热成像 384×288
D435 彩色 1280×720
```

改变分辨率、裁剪、镜头位置或两台相机相对安装关系后，需要重新标定。

### 标定后如何提交 Git

先查看新矩阵和误差日志，确认验证通过后再提交：

```bash
cd ~/match_ws
git diff -- src/uvc_ubuntu/config/thermal_d435_fusion.yaml
git add src/uvc_ubuntu/config/thermal_d435_fusion.yaml
git commit -m "更新热成像到D435单应矩阵标定"
```

