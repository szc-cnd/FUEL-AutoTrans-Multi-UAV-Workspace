# 项目协作规则

## UAV0 远端连接

- UAV0 的 SSH 账号为 `asus`，地址为 `10.32.24.212`，连接目标写作 `asus@10.32.24.212`，不得误用 `uav0` 或 `oem` 作为账号。
- UAV0 的项目工作空间为 `/home/asus/match_ws`，预期工作分支为 `vehicle/cav0`；任何远端修改前必须重新确认实际工作空间、当前分支和工作区状态。
- 当前可用的免密登录命令为 `ssh -i /home/oem/.ssh/id_ed25519_github_uav2 asus@10.32.24.212`。不得把 SSH 密码写入仓库、脚本、提交说明或日志。
- 2026-08-24 最近一次只读核对时，UAV0 的 HEAD 为 `4f81919`；该提交号仅用于识别当时状态，不得假定为永久固定版本，后续连接必须以远端实时 `git rev-parse --short HEAD` 为准。
- 连接 UAV0 不等于授权修改。用户只要求连接或检查时必须保持只读；只有用户明确要求修改、编译、提交或同步时，才执行对应远端操作。

## Git 本地提交

- 每次修改或回退源码、配置、launch、脚本后，提交前必须重新编译受影响的 ROS 包并执行与改动风险相符的测试或启动解析检查；编译或验证失败时不得将任务标记为完成，也不得提交未经验证的版本。
- 凡是修改会影响编译产物的源码、头文件、消息定义、CMake 文件或构建配置，修改完成后必须明确识别并编译全部受影响的 ROS 包，不得以静态检查或仅运行测试代替编译。
- 编译必须更新实际启动脚本使用的当前工作空间 `devel`；临时或隔离工作空间只能作为补充验证，不能代替主工作空间编译。提交前必须确认相关可执行文件或库已成功生成，且产物时间不早于本次修改。
- 每次完成源码、配置、launch 或脚本修改并完成相应验证后，必须在当前任务结束前自动创建本地 Git 提交，无需再次询问用户。
- Git 提交说明必须使用中文，简洁说明本次修改的目的或结果。
- 每个提交只包含当前任务产生的改动，不得夹带工作区中用户已有或与当前任务无关的修改、生成文件及临时文件。
- 提交前必须检查 `git diff`、`git diff --check` 和待提交文件列表；按任务范围显式暂存文件，禁止直接使用会纳入全部改动的暂存方式。
- 同一文件同时包含当前任务与用户既有改动时，必须保留用户改动，并只暂存能够可靠归属于当前任务的部分；无法可靠拆分时先向用户说明，不得擅自夹带提交。
- 完成修改后只创建本地 Git 提交；默认不得执行 `git fetch`、`git pull`、`git push` 或其他远端同步操作。
- 只有用户明确要求同步远端时，才允许检查远端状态并推送；禁止使用 `--force` 或等效强制推送。
- 用户要求同步远端但遇到分叉、目标不明确、网络、认证或 DNS 失败时，保留本地提交并报告，不得擅自改写历史或反复重试。
