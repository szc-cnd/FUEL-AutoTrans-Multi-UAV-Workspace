# CAV0 项目协作规则

每次修改完代码后都帮我自动提交本地仓库

## 修改原则

- 优先定位并修复根因，以满足当前需求的最小改动为默认方案，不顺带扩大修改范围。
- 能复用现有逻辑、状态和参数时不新增实现；能删除冲突逻辑或直接简化时，不叠加新的状态机、二次确认、门槛或约束。
- 优先采用简单、清晰、可验证的控制流程；只有安全底线或明确需求确有必要时才增加复杂约束，并在修改前说明原因和影响。
- 规划器、控制器及其他模块分开处理，未经明确要求不跨模块联动修改。

## Git 安全

- 不使用 `git reset --hard`、强制推送或重写远程历史。
- 不提交密码、令牌、SSH 私钥或 GitHub 凭据。
- 不把实验数据和构建产物混入源码提交。
- CAV0 上无法快进拉取时，先停止同步并检查本地与远程差异。

## 提交说明

- 每次 Git 提交的标题和说明正文必须使用中文；命令、文件路径、参数名、类名和函数名等技术标识可以保留原文。
- 提交前确认提交说明准确描述本次改动，不使用纯英文提交说明。

## 编译验证

- 修改 C/C++ 源码、头文件、`CMakeLists.txt`、`package.xml`、消息或服务定义，以及其他会影响编译产物的文件后，必须在提交前编译受影响的包或目标。
- 编译范围应覆盖修改实际影响的目标；涉及公共接口、跨包依赖或不确定影响范围时，应扩大到相关依赖包或工作区构建。
- 仅运行语法检查、静态检查或单元测试不能替代必要的编译。编译完成后仍应按改动风险运行相关测试和配置检查。
- 如果因环境、依赖或设备条件无法完成编译，必须保留完整错误信息并在任务结果中明确说明，不得把“未编译”表述为验证通过。
