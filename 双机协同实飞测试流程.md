# UAV0/UAV1 双机协同测试流程

## 1. 当前协同结构

- UAV0（`192.168.31.21`）是唯一 ROS Master，运行 FAST-LIO、FUEL、检测、双机降落协调器和 AutoTrans。
- UAV1（`192.168.31.163`）连接 UAV0 Master，运行自身 FAST-LIO、航点接力管理器、Diff-Planner、搜索精降和 AutoTrans。
- UAV0 根据实飞路线生成接力点，但只有两机对齐后的实时水平距离达到 `1.00 m` 才允许向 UAV1 释放普通航点。
- UAV1 只执行 `/UAV1/planning/goal` 外部目标，不执行 `run_swarm.launch` 中的单机预设航点。
- LDOP 动态障碍检测默认关闭；两机仍使用点云占据地图进行静态避障。
- 搜索降落默认待命，由 UAV0 遥控器 CH9 的低到高触发，不由出口误检自动触发；
  CH9 可直接让 FUEL 从 `WAIT_TRIGGER` 进入搜索降落，无需预先发送入口航点。
- 两机 AutoTrans 起飞目标统一为 FAST-LIO 世界坐标系 `z=0.60 m`，爬升参考速度为 `0.15 m/s`。
- 两机的起飞、普通规划轨迹和搜索降落 Diff 轨迹均由 AutoTrans 跟踪；UAV0 在 CH9 后把 AutoTrans 的轨迹输入从 FUEL 一次性切换到 Diff，旧简单控制器不参与正式双机流程。到达 ArUco 上方后仍由专用精降控制器安全接管最后下降。
- CH9 后仅 UAV0 搜索两个降落 ArUco。UAV0 先保持 CH9 触发瞬间的航向和高度向正前方移动 `1.00 m`，到达后才启动前视检测和偏航扫描。若前视不足两个码，UAV0 实测升至至少 `1.80 m` 并进入下视搜索后，UAV1 才前往这个前移后的前视扫描锚点，以名义 `z=0.60 m` 锁点等待；若前视已完成双码分配，UAV1 不提前移动。UAV0 到达自身平台上方并发布 `/UAV0/mission/landing_request=true`、准备精降时，协调器即发布 `/dual_uav_landing/release_uav1=true`，UAV1 随即前往自己的平台，无需等待 UAV0 完全降落。

## 2. 测试前安全条件

第一次检查必须拆桨进行。装桨测试前同时满足：

1. 两台电脑连接同一 Wi-Fi，UAV0 地址为 `192.168.31.21`，UAV1 地址为 `192.168.31.163`。
2. UAV0、UAV1 能双向 Ping；新终端中的 `ROS_MASTER_URI` 都是 `http://192.168.31.21:11311`。
3. 两架飞控分别只连接自己的 MAVROS，命名空间必须是 `/UAV0/mavros` 和 `/UAV1/mavros`。
4. CH8、CH9、CH10 先保持低位；启动脚本不会自动解锁、切换 OFFBOARD 或发布任务目标。
5. 实测两机出生点和航向，并核对 `src/control/config/dual_uav_frame_alignment.yaml`。当前标定为 UAV1 在 UAV0 后方 `1.20 m`、航向差 `0 rad`，不能把“航点释放距离 1 m”误当成坐标偏移。
6. 两架下视相机都存在，且各自的 `~/.ros/camera_info/down_camera.yaml` 可读取。
7. 不启动 `run_uav1_ldop.sh`，不在 UAV1 手工启动 `diff_planner run_swarm.launch`。

## 3. 启动顺序

### 3.1 UAV0 主六分屏

在 UAV0 普通终端执行：

```bash
cd /home/asus/match_ws
bash shfiles/start_uav0_dual_network_six_terminator.sh
```

该入口将 AutoTrans 输出改接到 `/UAV0/control/attitude_setpoint`。搜索降落仲裁器启动后，再由它唯一转发到 `/UAV0/mavros/setpoint_raw/attitude`。

### 3.2 UAV0 检测与搜索降落终端

另开一个 UAV0 终端执行：

```bash
cd /home/asus/match_ws
bash shfiles/start_uav0_first_seven_terminator.sh --pane detection
```

该终端等待 UAV0 FAST-LIO 后启动 D435、颜色标签、二维码、热源、下视相机、精降节点和唯一一份 `dual_uav_landing_coordinator`。

### 3.3 UAV1 下视相机终端

在 UAV1 先执行：

```bash
cd /home/oem/match_ws
sh shfiles/run_uav1_sensor_stack.sh landing
```

该命令只启动 UAV1 下视相机；搜索和精降节点由 UAV1 第 5 分屏启动，禁止重复启动。

需要在 UAV1 查看下视画面时，再开一个图形终端执行：

```bash
cd /home/oem/match_ws
bash shfiles/view_uav1_down_camera.sh
```

查看器会等待 `/UAV1/landing/combined_debug_image` 出现后自动打开独立的
`rqt_image_view` 窗口。搜索阶段显示 ArUco 搜索框、ID 和状态，精降接管后自动切换到
精降标注画面；它只订阅图像，不会重复启动相机、搜索、精降或控制节点。若只需检查
相机原始画面，可执行 `bash shfiles/view_uav1_down_camera.sh raw`。

### 3.4 UAV1 主六分屏

确认 UAV0 Master 已运行后，在 UAV1 另一个终端执行：

```bash
cd /home/oem/match_ws
bash shfiles/start_uav1_six_terminator.sh
```

UAV1 第 5 屏启动 `leader_safe_path_follower.launch`，其中包含坐标对齐、外部目标模式 Diff、航点接力和搜索精降；第 6 屏等待降落仲裁器后才启动 AutoTrans。
搜索和最终精降节点都会在共享基础配置之后加载
`src/precision_landing/config/down_camera_extrinsic_uav1.yaml`，因此 UAV1 使用自身的
下视外参，UAV0 的外参数值保持不变。

## 4. 拆桨状态下的 ROS 图检查

在任意新终端执行：

```bash
rosnode list | grep -E 'UAV0|UAV1|leader_safe_path|dual_uav|drone_[01]|mpc_controller'

rostopic hz /UAV0/fast_lio/Odom_high_freq
rostopic hz /UAV1/fast_lio/Odom_high_freq
rostopic hz /UAV0/fast_lio/cloud_registered
rostopic hz /UAV1/fast_lio/cloud_registered
rostopic hz /drone_0_traj_server/heartbeat
rostopic hz /drone_1_traj_server/heartbeat
```

必须存在以下关键节点：

```text
/UAV0/mavros
/UAV1/mavros
/UAV0/laserMapping
/UAV1/laserMapping
/UAV0_livox_imu_to_body
/UAV1_livox_imu_to_body
/leader_safe_path_follower
/dual_uav_frame_alignment
/dual_uav_landing_coordinator
/drone_1_diff_planner_node
/drone_1_traj_server
/mpc_controller_node
/UAV1/mpc_controller_node
```

检查控制输出所有权：

```bash
rostopic info /UAV0/mavros/setpoint_raw/attitude
rostopic info /UAV1/mavros/setpoint_raw/attitude
rostopic echo -n1 /UAV0/landing/control_owner
rostopic echo -n1 /UAV1/landing/control_owner
```

正常规划阶段，两个 `control_owner` 都应为 `CONTROLLER`；MAVROS attitude 话题的唯一发布者应分别是两机的 `landing_setpoint_arbiter`，AutoTrans 发布在 `/UAV0/control/attitude_setpoint` 和 `/UAV1/control/attitude_setpoint`。若 AutoTrans 仍直接出现在 MAVROS attitude 的发布者列表中，禁止解锁。

确认没有共享 Master 节点名冲突：

```bash
rosnode list | sort | uniq -d
```

该命令必须没有输出；公共 RViz 只保留 UAV0 的一份。

## 5. 航点接力测试

先拆桨验证话题连接，再进行低高度实飞：

1. 分别确认两机 MAVROS、FAST-LIO、视觉位姿和控制器状态正常。
2. 两机分别完成起飞/悬停准备，未发送 UAV0 FUEL 目标前，UAV1 不应收到接力目标。
3. 给 UAV0 发送入口/FUEL 任务目标。
4. 观察以下话题：

```bash
rostopic echo /leader_safe_path_follower/state_marker
rostopic echo /UAV0/mission/relay_waypoints
rostopic echo /UAV1/planning/goal
rostopic echo /drone_1_planning/status
rostopic echo /UAV1/planning/pos_cmd
```

5. 两机实际水平距离不足 `1.00 m` 时，日志应显示 `HOLD ... actual ... separation`，不得发布新的普通接力目标。
6. 达到 `1.00 m` 且 UAV0 已沿安全路线清空相应距离后，UAV1 才收到离散目标，并由自身 Diff 生成轨迹。
7. `/UAV1/planning/pos_cmd` 只能由 `/drone_1_traj_server` 发布；`/UAV1/mavros/setpoint_raw/attitude` 最终只能由 UAV1 降落仲裁器发布。

## 6. 搜索降落协同测试

航点接力单独验证通过后再测试本阶段：

1. 保持 CH9 低位启动全部节点；确认不会因出口误检自动进入搜索降落。CH9 必须在启动后先被 MAVROS 读到低位（`<=1300`），再拨到高位（`>=1800`）并保持至少 `0.5 s`。
2. 确认两个下视图像和相机内参持续有效：

```bash
rostopic hz /UAV0/down_camera/image_raw
rostopic hz /UAV1/down_camera/image_raw
rostopic echo -n1 /UAV0/down_camera/camera_info
rostopic echo -n1 /UAV1/down_camera/camera_info
```

UAV1 图形终端中的独立查看窗口应持续显示下视画面；若尚未启动搜索精降节点，可先用
`bash shfiles/view_uav1_down_camera.sh raw` 检查原始图像。

3. 确认两机 FAST-LIO、MAVROS、规划器和控制器均已就绪。无需发送 UAV0 入口航点，人工拨动 UAV0 CH9 低到高并保持至少 `0.5 s`，观察：

```bash
rostopic echo /UAV0/mission/task_status
rostopic echo /dual_uav_landing/status
rostopic echo /dual_uav_landing/assignments_ready
rostopic echo /dual_uav_landing/release_uav1
rostopic echo /autotrans_trajectory_arbiter/owner
rostopic echo /landing_diff_search_manager/state
rostopic echo /UAV0/landing/front_aruco_hint
rostopic echo /UAV0/landing_diff/subgoal
```

   CH9 前 `/autotrans_trajectory_arbiter/owner` 应为 `FUEL`，触发后必须锁存为 `DIFF`；两阶段最终都进入同一个 UAV0 AutoTrans，不启动简单控制器。

   CH9 触发后，`/landing_diff_search_manager/state` 必须先进入 `FRONT_ARUCO_FORWARD_APPROACH`。UAV0 保持触发瞬间的航向和高度，由 Diff 向正前方发布 `1.00 m` 目标；实测水平误差进入 `0.15 m` 后，才发布 `/UAV0/landing/front_scan_anchor` 并进入 `FRONT_ARUCO_INITIAL_WAIT`。移动期间前视图像不参与 ArUco 候选累计。

   到达前移锚点后，UAV0 再原地执行前视偏航扫描：保持 XYZ 不变，按“CH9 锁定航向→左侧约 `45°`→右侧约 `45°`→回到 CH9 锁定航向”转动。AutoTrans 在这些前视扫描状态中会锁存到达锚点时的实际位置、清除旧的平移轨迹，只跟踪 `/UAV0/landing_diff/yaw`。每个扫描端点都要求 FAST-LIO 实测航向误差不超过 `0.12 rad`（约 `6.9°`）才允许进入下一段；若未找齐两个 ArUco，必须先实际回到 CH9 锁定航向才能进入下视蛇形搜索。下视搜索和平台接近期间，AutoTrans 的轨迹航向参考也持续固定为 CH9 锁定航向，直到精降接管。

   实飞时可用以下话题确认状态和航向指令：

```bash
rostopic echo /landing_diff_search_manager/state
rostopic echo /UAV0/landing_diff/yaw
```

4. 若前视扫描完成时不足两个码，`/landing_diff_search_manager/state` 应进入 `FRONT_ARUCO_YAW_SCAN_COMPLETE_START_DOWN_SWEEP` 或单码下视确认状态；只有 UAV0 实测高度达到 `1.80 m` 且与 UAV1 的 `0.60 m` 等待层保持至少 `1.00 m` 垂直间距后，UAV1 才收到前视扫描锚点目标。UAV1 到达后状态应为 `DIFF_WAIT_FRONT_SEARCH_ANCHOR`。若前视已完成双码分配，则不得触发该提前移动。
5. UAV0 确认两个平台后，远平台分配给 UAV0、近平台分配给 UAV1；UAV1 即使已经到达前视锚点，也必须继续锁点，直到 UAV0 到达自身平台并准备精降。
6. UAV0 发布 `/UAV0/mission/landing_request=true` 后，协调器立即发布 `/dual_uav_landing/release_uav1=true`；`/UAV0/landing/success` 仅在请求话题异常缺失时作为兜底释放条件。
7. 收到上述释放信号后，UAV1 从当前等待点执行分配到的近平台目标，不等待 UAV0 完全降落。
8. 精降接管后，对应 `/UAVx/landing/control_owner` 必须锁存为 `LANDING`，AutoTrans 控制输入不再转发到 MAVROS。

## 7. 立即中止条件

出现以下任一情况，保持人工接管并禁止进入 OFFBOARD：

- 任一 FAST-LIO、MAVROS 或规划心跳中断；
- 两机坐标对齐方向或距离与实测不符；
- 同一控制/规划话题出现两个发布者；
- `/UAV1/planning/goal` 在未满足 1 m 间距时出现；
- UAV1 启动了预设航点 `run_swarm.launch`；
- MAVROS attitude setpoint 绕过降落仲裁器；
- 搜索降落在 CH9 未触发时自行启动；
- Wi-Fi 持续丢包或延迟抖动导致话题明显超时。

飞行中不要通过关闭 ROS Master 作为常规中止手段，应优先用遥控器退出 OFFBOARD/切换人工或按既定降落通道处置。

## 8. 当前已知边界

- 动态障碍检测已关闭；移动物体只可能作为普通点云占据进入静态地图。
- 当前 Wi-Fi 实测无丢包，但曾出现约 `150–170 ms` 延迟峰值；低频航点信号可传输，高频状态仍需现场持续观察。
- `dual_uav_frame_alignment.yaml` 的 `-1.20 m` 是当前配置值，不代表已经完成当天场地标定。
- 第一次装桨测试只验证低高度、单个接力点；通过后再增加路线长度，最后才测试搜索降落。
- Diff→AutoTrans 轨迹失联阈值为 `2.0 s`。
