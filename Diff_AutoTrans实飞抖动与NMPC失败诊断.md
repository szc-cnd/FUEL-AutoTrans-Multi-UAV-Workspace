# Diff-Planner + AutoTrans 实飞抖动与 NMPC 求解失败诊断

## 1. 文档范围

本文整理 2026-08-14 最新一次实飞中出现的以下问题：

- 无人机容易突然翘头，随后反向压回，形成前后摇摆。
- 无人机在水平面左右来回抖动。
- NMPC 连续出现一次错误码 58 和两次错误码 33。
- NMPC 安全恢复后，Diff 轨迹继续沿旧预测状态前进，与实际无人机脱节。

本次分析使用的主要日志：

- ROS 日志：`/home/oem/.ros/log/19bd42be-97e3-11f1-adbf-0d1eb59764cb/rosout.log`
- 控制器设定值：`/home/oem/.ros/autotrans_mpc_logs/20260814_212227__unlabeled__comp_off/20260814_212227__unlabeled__comp_off_setpoint.csv`
- Diff 原始轨迹：`/home/oem/.ros/autotrans_mpc_logs/20260814_212227__unlabeled__comp_off/planner_trajectory.csv`
- 桥接后轨迹：`/home/oem/.ros/autotrans_mpc_logs/20260814_212227__unlabeled__comp_off/trajectory.csv`

## 2. 总体结论

这次抖动不是单一参数导致，而是以下问题形成了闭环放大：

1. NMPC 暂停执行并安全悬停时，Diff 不知道控制器已经暂停，仍沿旧预测轨迹继续规划。
2. 恢复后的新轨迹可能从无人机前方开始，位置和速度参考发生明显跳变。
3. NMPC 位置误差权重较高、角速度惩罚较低，并允许较大的水平角速度，因此会猛烈追赶前方轨迹。
4. `use_fix_yaw=false` 时，偏航由轨迹速度方向生成；重规划、低速和横向脱障会使速度方向突变，进而造成偏航参考大幅跳变。
5. Diff 发布的部分多项式轨迹实际速度超过 NMPC 的硬速度约束，实际无人机速度也发生过短时越界，最终造成 QP 求解失败。

主要因果链为：

```text
重规划/局部脱障/求解失败
        ↓
控制器进入安全悬停，Diff 继续推进预测轨迹
        ↓
恢复后轨迹起点、速度或偏航与实际状态不连续
        ↓
高增益 NMPC 使用大角速度追赶
        ↓
翘头、左右摆动、速度超调
        ↓
触碰 NMPC 硬约束，QP 再次失败
```

## 3. 翘头问题

### 3.1 关键数据

第三次 NMPC 失败后，控制器锁存的悬停点约为：

```text
(3.675, -0.671, 0.292)
```

约 0.09 秒后，控制器退出安全恢复。随后收到的 ID 35 轨迹起点为：

```text
(3.952, -0.697, 0.377)
```

该轨迹到达时：

```text
轨迹起点与实际位置差：0.224 m
轨迹起点与实际速度差：0.424 m/s
```

因为 `0.224 m < max_tracking_error=0.30 m`，Diff 没有触发从实际里程计重新规划。

随后控制器出现：

```text
俯仰角速度指令最大：+3.0 rad/s
反向俯仰角速度指令：约 -2.8 rad/s
实际最大俯仰角：0.247 rad，约 14.2°
当时轨迹参考俯仰：约 -0.5°
```

### 3.2 原因判断

轨迹本身没有要求无人机抬头 14.2°。大俯仰是 NMPC 为追赶位于前方的轨迹位置和速度参考而产生的控制动作。

当前主要参数为：

```yaml
Q_pos_xy: 250.0
Q_velocity: 25.0
R_pitchroll: 2
max_bodyrate_xy: 3.0
```

位置误差代价较高、角速度代价较低、水平角速度上限又较大，因此轨迹一旦领先，NMPC 会采用激烈姿态动作追赶。

## 4. 水平面左右抖动

### 4.1 偏航参考跳变

当前设置为：

```yaml
use_fix_yaw: false
```

控制器根据轨迹水平速度方向计算偏航。日志中发现：

```text
参考偏航单周期最大正跳变：+104°
参考偏航单周期最大负跳变：-118.5°
最大偏航误差：120.7°
偏航角速度达到 ±1.2 rad/s 上限：305 个控制周期
```

相关代码每次构造预测参考时，会从轨迹切线重新初始化 `last_yaw_`。当 Diff 重规划、低速转向或执行横向脱障时，新轨迹速度方向可能与上一时刻差异很大。

横向脱障本应只产生平移；但在切线偏航模式下，无人机还会尝试把机头转向横移方向。偏航快速变化后，世界系横向修正会不断在机体系横滚和俯仰之间转换，从而表现为左右摆动。

### 4.2 横滚和横向误差

本次日志统计：

```text
实际横滚范围：约 -6.2° 至 +6.0°
横滚角速度最大：2.10 rad/s
横滚角速度超过 1 rad/s：17 个控制周期
横向位置误差最大：约 0.12 m
```

### 4.3 轨迹切换不连续

部分明显的轨迹切换如下：

| 新轨迹 ID | 位置跳变 | 速度跳变 | 说明 |
|---:|---:|---:|---|
| 33 | 0.360 m | 0.459 m/s | 从旧轨迹切换到里程计重规划轨迹 |
| 35 | 与实际位置差 0.224 m | 与实际速度差 0.424 m/s | 安全恢复后仍从旧预测状态起步 |
| 36 | 0.353 m | 0.148 m/s | 跟踪偏差超过阈值后重新锚定里程计 |

部分正常重规划虽然位置、速度和加速度基本连续，但 jerk 跳变最大约为 `1.26 m/s³`。在当前偏激进的控制权重下，jerk 跳变也会放大为短时姿态角速度脉冲。

## 5. 三次 NMPC 求解失败

### 5.1 第一次：错误码 58

日志含义：

```text
Maximum number of working set recalculations performed
```

当时状态和轨迹：

```text
实际 vx：0.220 m/s，尚未超过 0.30 m/s 硬约束
偏航角速度：已经达到 -1.2 rad/s 上限
ID 32 轨迹内部最大 vx：0.354 m/s
NMPC 水平速度硬约束：0.30 m/s
```

这是工作集迭代未能收敛，不等同于已经严格证明无解。轨迹速度超过可行域、偏航角速度处于边界，加上较高状态权重，使多个约束频繁进入和退出激活集合，最终达到 qpOASES 的工作集重计算上限。

### 5.2 第二次：错误码 33

日志含义：

```text
Initial QP could not be solved due to infeasibility
```

ID 33 的起点与里程计本身较接近：

```text
起点位置误差：0.006 m
起点速度误差：0.023 m/s
```

但这条轨迹同时存在两个问题：

```text
起点速度方向：约 -134°
无人机实际偏航：约 +6°
偏航参考瞬间差异：约 140°
轨迹内部最大 vx：0.491 m/s
```

在 `use_fix_yaw=false` 时，速度方向直接变成偏航参考。这条轨迹要求大幅掉头，同时后续速度明显超过 `0.30 m/s` 的 NMPC 硬约束，导致初始 QP 不可行。

### 5.3 第三次：错误码 33

第三次失败时实际状态为：

```text
vx = 0.323 m/s
vy = -0.100 m/s
vz = 0.157 m/s
```

NMPC 硬约束为：

```text
|vx| <= 0.30 m/s
|vy| <= 0.30 m/s
|vz| <= 0.20 m/s
```

此时实际 `vx=0.323 m/s` 已经越界。NMPC 同时要求初始预测状态等于实际状态，并要求所有状态满足速度硬约束，因此初始状态与硬约束直接冲突，返回错误码 33。

## 6. 配置不匹配

`run_swarm.launch` 中 Diff 的规划速度设置为：

```xml
<arg name="max_vel" value="0.25" />
```

但日志中的多项式轨迹仍出现：

```text
ID 32 最大 vx：0.354 m/s
ID 33 最大 vx：0.491 m/s
ID 34 最大 vx：0.399 m/s
ID 35 最大 vx：0.396 m/s
```

说明 `max_vel=0.25` 没有对最终发布的完整多项式形成可靠的硬限制，尤其是从实际速度初始化、紧急停止、局部恢复和重新计时后的轨迹。

与此同时，NMPC 使用 `max_velocity_xy=0.30` 硬约束。规划轨迹超过该限制，实际跟踪又可能短时超调，因此求解器缺少必要的可行余量。

## 7. 建议修改方案

### 7.1 第一优先级：修复规划器与控制器恢复握手

原版 AutoTrans 只记录 ACADO/qpOASES 的失败返回值，上层没有完整的恢复握手。当前 Diff + AutoTrans 实机组合应增加如下闭环：

```text
NMPC 失败
  → 控制器只锁存一次当前位置和航向
  → 清空执行中轨迹，进入 MPC_RECOVERY_HOVER
  → 故障期间控制器直接丢弃规划轨迹
  → 控制器使用固定悬停点重置并测试 NMPC
  → NMPC 在恢复窗口内有一次完整求解成功
  → 控制器发布 /planning_restart_trigger
  → Diff 保留当前目标和航点，从最新里程计位置、速度强制规划
  → 控制器只接受恢复时间之后生成的新轨迹
  → 新轨迹到达后退出悬停并恢复 POLY_TRAJ
```

#### 7.1.1 进入恢复

控制器第一次检测到 NMPC 求解失败时执行：

1. 只锁存一次 `hover_pose` 和 `hover_yaw`，后续恢复周期不得刷新悬停点。
2. 清空当前轨迹及故障期间收到的轨迹，进入 `MPC_RECOVERY_HOVER`。
3. 完整重置 ACADO/NMPC 求解器，并用锁存悬停参考测试恢复。
4. 不新增规划器暂停状态；故障期间新轨迹由控制器入口直接丢弃。

#### 7.1.2 求解器单次恢复成功

控制器在恢复窗口内第一次完整求解成功后，即认为求解器已经恢复。但此时不立即回到原轨迹，而是：

1. 保持锁存悬停点，继续处于 `HOVER`。
2. 清除故障期间缓存的全部轨迹。
3. 记录恢复完成时间，只允许更新且时间戳晚于该时刻的轨迹进入校验。
4. 发布一次 `/planning_restart_trigger`。

该话题直接使用现有发布器：

```text
/planning_restart_trigger  std_msgs/Empty
```

#### 7.1.3 Diff 强制从里程计重规划

Diff 收到 `/planning_restart_trigger` 后执行：

1. 保留当前任务目标和预设航点编号。
2. 清除规划失败计数并刷新 stuck detect 豁免时间。
3. 若当前位置自由，切换到 `GEN_NEW_TRAJ`；该状态现有的 `planFromGlobalTraj()` 会无条件使用最新里程计位置、速度和零加速度作为起始状态。
4. 若当前位置仍在膨胀障碍内，进入现有 `EMERGENCY_STOP → OCCUPIED_RECOVERY` 局部脱障流程。
5. 新轨迹发布成功后清除控制器重启等待标志。

如果里程计无效、起点仍处于障碍物中或强制规划失败，Diff 不得恢复旧预测轨迹；应保持暂停并返回失败状态，让控制器继续安全悬停，直至恢复超时进入 `AUTO.LAND`。

#### 7.1.4 控制器恢复轨迹时间门控

恢复完成时清空故障期间缓存的全部轨迹，并记录恢复时间。控制器只接受 `header.stamp` 晚于恢复时间的新轨迹；不增加轨迹起点位置差或速度差门槛。新轨迹到达前继续保持原锁存悬停点。

#### 7.1.5 恢复超时

如果在恢复超时时间内无法得到稳定 NMPC 解和合格的新轨迹：

1. 向 `/mandatory_stop_to_planner` 发布永久停止信号。
2. 禁止继续接收任何规划轨迹。
3. 请求 PX4 `AUTO.LAND`，并按配置周期重试直到飞控确认。

这里需要把“求解器恢复成功”和“任务恢复成功”区分开：一次完整成功求解只代表 NMPC 当前可以计算悬停控制量；只有 Diff 从实时里程计生成的新轨迹到达，任务才真正恢复。

### 7.2 第二优先级：普通跟踪偏差重锚定

`run_swarm.launch` 的普通飞行三维跟踪偏差阈值设置为：

```xml
<arg name="max_tracking_error" default="0.20"/>
```

该阈值只用于普通轨迹执行期间决定何时放弃旧预测状态并从里程计重锚定。控制器不增加新轨迹起点位置差或速度差检查。

### 7.3 第三优先级：简化版连续偏航

当前任务希望机头继续跟随轨迹，因此首轮建议采用改动较小的连续偏航方案，而不是直接固定偏航：

```yaml
use_fix_yaw: false
max_bodyrate_z: 0.5
```

保留原有的 `atan2(vy, vx)` 航向目标计算，但做以下修改：

1. 删除每次收到新轨迹或开始构造新预测窗口时重新设置 `last_yaw_` 的逻辑。
2. `last_yaw_` 和 `last_yaw_dot_` 跨控制周期、跨轨迹 ID 持续保存；仅在控制器首次初始化、重新起飞或安全恢复锁存时由实际偏航初始化。
3. 水平速度低于 `0.12 m/s` 时保持当前偏航，避免里程计和轨迹微小速度噪声导致左右摆头。
4. 将偏航参考角速度限制为 `±0.5 rad/s`。

简化更新逻辑如下：

```cpp
if (vel.head<2>().norm() > 0.12) {
  target_yaw = std::atan2(vel.y(), vel.x());
} else {
  target_yaw = last_yaw_;
}

const double yaw_error = angle_limit(target_yaw - last_yaw_);
const double yaw_rate = std::clamp(yaw_error / dt, -0.5, 0.5);
last_yaw_ = angle_limit(last_yaw_ + yaw_rate * dt);
last_yaw_dot_ = yaw_rate;
```

生成 NMPC 整个预测窗口时必须使用 `last_yaw_`、`last_yaw_dot_` 的局部副本向前计算。全局偏航状态每个控制周期只推进一次，不能在预测窗口循环中推进二十次，也不能把窗口末端的预测偏航写回全局状态。

这个简化版不包含 `0.8 s` 前视和偏航角加速度限制，平滑程度略低，但能先解决大部分重规划瞬间摆头和低速左右摇头问题，适合作为第一轮试飞方案。如果仍有明显偏航冲击，再升级到“轨迹位置前视 + 偏航角加速度限制”的完整方案。

如果现场测试更重视狭窄空间稳定性、不要求机头跟随轨迹，可随时退回固定偏航：

```yaml
use_fix_yaw: true
```

### 7.4 第四优先级：给 NMPC 保留可行余量

规划器仍保持目标速度 `0.25 m/s`，NMPC 硬约束可先调整为：

```yaml
max_velocity_xy: 0.5
max_velocity_z: 0.3
```

放宽 NMPC 约束不是让规划器以 `0.5 m/s` 飞行，而是允许实际速度短时超调，使初始状态仍在求解器可行域内。Diff 的目标速度仍保持 `0.25 m/s`。

本轮不增加 Diff 轨迹发布前的速度、加速度、jerk 检查或自动时间缩放。

### 7.5 第五优先级：降低控制攻击性

完成状态连续性和速度约束修复后，再进行参数整定。第一轮建议：

```yaml
Q_pos_xy: 150.0
Q_velocity: 15.0
R_pitchroll: 5.0
max_bodyrate_xy: 1.5
max_bodyrate_z: 0.5
```

其中前四项作为减少翘头和水平摆动的首轮核心调整：

| 参数 | 当前值 | 首轮建议值 | 作用 |
|---|---:|---:|---|
| `Q_pos_xy` | 250.0 | 150.0 | 降低控制器追赶水平位置误差的激烈程度 |
| `Q_velocity` | 25.0 | 15.0 | 降低速度参考突变引发的加减速冲击 |
| `R_pitchroll` | 2.0 | 5.0 | 提高横滚、俯仰角速度控制动作的代价 |
| `max_bodyrate_xy` | 3.0 rad/s | 1.5 rad/s | 对翘头和左右猛摆设置直接硬限幅，同时保留必要的横向纠偏能力 |

`Q_attitude_rp` 第一轮先保持不变，避免一次修改过多参数导致无法判断效果。

复测后的微调原则：

- 如果仍明显翘头或左右打摆，将 `R_pitchroll` 增加到 `6～8`。
- 如果跟踪明显过慢，先将 `Q_pos_xy` 增加到 `180`；首轮复测阶段保持 `max_bodyrate_xy=1.5 rad/s`，避免同时放宽角速度上限。
- 如果轨迹连续性修复后仍存在到点刹不住，可将 `Q_velocity` 调回 `18～20`。
- 不建议在当前室内任务中恢复 `max_bodyrate_xy=3.0 rad/s`。

参数调整只能降低问题的冲击，不能替代轨迹连续性、恢复握手和速度可行性修复。

## 8. 推荐实施顺序

1. 修复 NMPC 恢复与 Diff 的 restart 握手，恢复后强制从实时里程计重规划。
2. 实现跨重规划连续偏航，并限制低速摆头和最大偏航角速度。
3. 为 NMPC 速度硬约束保留实际超调余量。
4. 降低水平角速度上限并调整 MPC 权重。

## 9. 复测验收标准

- NMPC 一次完整成功求解后只发布一次 `/planning_restart_trigger`。
- 恢复期间及恢复时间之前生成的轨迹均不会执行。
- Diff 收到 restart 后保留当前目标和航点，并从最新里程计重新规划。
- 实际速度短时超调时，不再因为初始状态越过 NMPC 硬边界而立即 QP 不可行。
- 新轨迹 ID 到达时偏航状态不重置，低速时保持航向。
- 偏航角速度不超过 `0.5 rad/s`，生成预测窗口时全局偏航每周期只推进一次。
- 水平角速度不再长时间触及限幅。
- 实际俯仰不再出现约 14° 的追赶性突变。
- 横滚和俯仰指令在轨迹切换前后保持连续，无明显正反向饱和。
- 单次 NMPC 故障恢复后，Diff 不再沿旧预测状态继续领先无人机。

## 10. 电池状态说明

本次飞行后段电池电压约 `14.3 V`，剩余电量日志从约 15% 降至 0%。低电量可能加重推力响应波动，但最大翘头事件与轨迹错位、角速度饱和在时间上直接对应，因此电池不是本次主要触发原因。复测仍应使用电量充足的电池，以排除供电因素。
